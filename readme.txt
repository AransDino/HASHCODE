1º Version - Se añade la hoja con los mulos de las funciones.
Se crea una funcion llamada justamente llamada(). No hay nada mas que las funciones de codificacion CRC32, MD5, SHA y base64.
Solo aplica la funcion a un campo determinado que posteriormente convertira en un HASH

2º Version - Se añade la fuente de codigos de barra y un boton para realizar la conversion y el mostrado del Hash en formato codigo de barras
Este hash viene de concatenar algunos de los campos basicos de la propia factura.

